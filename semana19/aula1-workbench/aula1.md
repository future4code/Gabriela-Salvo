### Exercício 1
```
a) VARCHAR são é usado para declarar strings. FLOAT declarar numeros não inteiros. 
DATE declarar datas. PRIMARY KEY numero indentificador. NOT NULL quando o valor nao pode ser vazio
b) o SHOW DATABASES retornou a os arquivos SHOW TABLES retornou a tabela Actor
c) O comando DESCRIBE descreveu os tipos e os valores da tabela e sua estrutura o SHOW Actor me retornou um erro.

```
### Exercicio 2

```
a)Deu erro de entrada duplicada '002'. Aconteceu porque colocamos o mesmo id para objetos diferentes.
b)O erro nos diz que os valores nao se correspondem (parâmetro)
c)Indica que falta um parâmetro a ser preenchido.
d)Indica que o campo id nao pode ser vazio (null)
e)Retorna um erro pois a data nao está como string. (retorna o valor 1950 porque o programa faz uma conta matemática de subtração dos valores)
```
### Exercicio 3

```
c) Retornou o erro => 0 row(s) returned
e)Retorna que não conhece a coluna "nome"

```

### Exercicio 4

```
a)Ela pega a tabela Actor e procura dentro dela palavras que comecem com % ou comecem com J e o salario seja maior que 30000.

```